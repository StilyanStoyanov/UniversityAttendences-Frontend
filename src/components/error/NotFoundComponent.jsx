import React, {Component} from 'react';

export default class NotFoundComponent extends Component{
    render(){
        return(
            <div>The page that you are searching for is unavailable</div>
        )
    }
}